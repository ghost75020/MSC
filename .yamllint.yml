---
extends: default

ignore: |
  ansible_env/
  actions-runner/
  .git/

rules:
  line-length:
    max: 160
  document-start:
    present: true
  comments:
    min-spaces-from-content: 1
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 0
  truthy: disable
